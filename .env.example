# Server Port (Optional, default: 3000)
# PORT=3000

# SQLite Database Path (Optional, default: storage.db)
# Used to persist user stop preferences. In Docker, mount a volume to /data
# DATABASE_PATH=/data/storage.db

# Alexa Request Verification (Required for Production: true)
# Set to 'false' for local testing to bypass signature validation.
# Set to 'true' in production to ensure requests come from Alexa.
VERIFY_SIGNATURE=true

# Developer Mode (Optional)
# Adds a prefix to Alexa responses for debugging purposes.
# IS_DEV=[DEV] 
